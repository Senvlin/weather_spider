[project]
name = "weather-spider"
version = "0.1.0"
description = "用于小组内部的天气信息爬取"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx==0.28.1",
    "parsel==1.11.0",
]

[tool.uv.workspace]
members = [
    "weather_spider",
]
